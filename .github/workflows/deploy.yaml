name: stage

on:
  push:

concurrency: stage


jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - name: test2
        run: echo "test2"

  call-mabl:
    needs: test 
    uses: ./.github/workflows/mabl.yaml 
