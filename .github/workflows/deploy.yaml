name: stage

on:
  push:

concurrency: stage


jobs:
  runs-on: ubuntu-latest
  test: 
    steps:
      - name: test3
        run: echo "test3"

  call-mabl: 
    uses: ./.github/workflows/mabl.yaml 
