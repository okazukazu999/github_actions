name: stage

on:
  push:

concurrency: stage


jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - name: test3
        run: echo "test3"

  call-mabl: 
    uses: ./.github/workflows/mabl.yaml 
